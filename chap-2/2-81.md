```lisp
; 1. The function will go into an infinite loop, constantly coercing the types
; 2. The function works correctly as is, just a bit inefficiently.
; 3. Slightly skipped. Before the if after the failed proc, just put in a test
;    of equality on the type1 and type2 variables.

```

> CORRECT
