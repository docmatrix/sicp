```lisp
#lang racket

; Just use tree->list, use the O(n) ordered set implementation of intersection-set and union-set
; and use list->tree again. It's 3N for each, but this is still O(n)

```

> CORRECT
